---
description: List CLI-It harnesses in this project and installed CLI-It tools
---

Execute `cli-it-plugin/commands/list.md`: scan for `*/agent-harness/`
directories, probe PATH for `cli-it` and `cli-it-*` entry points, cross-check
`registry.json` and `skills/cli-it-*/`, and report tables plus any
inconsistencies.
